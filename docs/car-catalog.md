# کاتالوگ خودرو

فهرست اولیه‌ای که در سامانه بار می‌شود. **این فهرست قرار است ویرایش شود** — از پنل مدیریت،
بدون اینکه کسی به کد دست بزند.

منبع: گزارش‌های بازار ایران در میانه‌ی ۱۴۰۵. لیست محصولات مدیران خودرو هر چند ماه عوض
می‌شود، پس این را نقطه‌ی شروع بدانید نه مرجع.

---

## چه چیزی بار شده

**شرکت:** مدیران خودرو

| برند | تعداد مدل | مدل‌ها |
|---|---|---|
| ام‌وی‌ام | ۷ | X22 پرو (دنده‌ای و اتوماتیک)، X33 کراس (دنده‌ای و اتوماتیک)، X55 پرو، X55 پرو IE، X77 |
| فونیکس | ۱۶ | FX، FX پریمیوم، FX EV، Z6 GT، Z8، Z8 GI، F7، F7 پرو پریمیوم، F7 پرو مکس، F7 پرو e هیبرید، F8، F8 پرو مکس، F8 پرو مکس AWD، F8 پرو مکس IE، F8 پرو e هیبرید، F9 |
| اکستریم | ۳ | LX، TXL، VX |

**رنگ‌ها (۱۲):** سفید، مشکی، خاکستری، نوک‌مدادی، نقره‌ای، تیتانیوم، آبی، قرمز، سبز،
قهوه‌ای، بژ، طوسی

---

## چرا لیست بسته، نه متن آزاد

اگر نوع خودرو متن آزاد بود، «فونیکس FX» و «فونیکس اف ایکس» و «Fownix FX» سه خودروی متفاوت
می‌شدند و هیچ‌کدام همدیگر را در فیلتر پیدا نمی‌کردند. با لیست بسته، فیلتر واقعاً کار می‌کند
و آمار پنل مدیریت معنی دارد.

**شرکت عرضه‌کننده هم دیگر ورودی نیست** — از روی مدل انتخاب‌شده خوانده می‌شود، پس هیچ آگهی
نمی‌تواند ادعا کند یک فونیکس را شرکت دیگری ساخته.

---

## قاعده‌ی اسنپ‌شات

نام مدل **روی خود حواله هم ذخیره می‌شود**، نه فقط ارجاع به جدول.

دلیلش: اگر سال بعد اسم یک مدل را اصلاح کنید، حواله‌های قدیمی باید همان چیزی را نشان دهند
که موقع ثبت نشان می‌دادند. وگرنه یک اصلاح ساده در کاتالوگ، متن آگهی‌های گذشته را بازنویسی
می‌کند — از جمله آگهی‌هایی که علیه‌شان گزارش تخلف ثبت شده و متن آگهی مدرک است.

به همین دلیل هم:

- **تغییر نام مدل** → آگهی‌های قبلی دست‌نخورده می‌مانند
- **غیرفعال کردن مدل** → آگهی جدید با آن ثبت نمی‌شود، ولی آگهی‌های موجود سالم می‌مانند

---

## ویرایش از پنل مدیریت ✅

انجام شد. مدیر کل می‌تواند شرکت، برند، مدل و رنگ **اضافه کند، اسمشان را عوض کند، ترتیبشان را
تغییر دهد و بازنشسته‌شان کند** — بدون هیچ نیازی به برنامه‌نویس.

| کار | چطور |
|---|---|
| افزودن مدل | `POST /admin/catalog/models` |
| تغییر نام یا ترتیب | `PATCH /admin/catalog/models/:id` |
| بازنشسته کردن | `PATCH` با `isActive: false` |
| دیدن تعداد آگهی‌های یک مدل | `GET /admin/catalog/models/:id/usage` |

**دکمه‌ی «حذف» عمداً وجود ندارد.** مدلی که استفاده شده، در آگهی‌ها و سابقه‌ی بازدید و
گزارش‌های تخلف ارجاع دارد؛ حذفش تاریخچه را با خودش می‌برد. «حذف خودرو» یعنی **غیرفعال کردن**:
آگهی جدید با آن ثبت نمی‌شود، آگهی‌های موجود دقیقاً همان‌طور که بودند می‌مانند. قبل از غیرفعال
کردن، تعداد آگهی‌های متأثر نشان داده می‌شود تا غافلگیر نشوید.

هر تغییر کاتالوگ در تایم‌لاین ثبت می‌شود؛ اگر روزی چیزی غیرمنتظره عوض شد، معلوم است کار کی بوده.

دسترسی: فقط **مدیر کل**. پشتیبانی و مالی نمی‌توانند — چون تغییر کاتالوگ تعیین می‌کند همه‌ی
نمایندگی‌ها چه چیزی می‌توانند آگهی کنند.

---

## ساختار جدول‌ها

سه جدول: `CarCompany` ← `CarBrand` ← `CarModel`، به‌علاوه‌ی `CarColor`.
هر کدام `isActive` و `sortOrder` دارند: `sortOrder` ترتیب نمایش در فرم را تعیین می‌کند.

فایل داده‌ی اولیه: `backend/src/constants/carCatalog.js`
بارگذاری: `npm run seed` (بی‌خطر است، چند بار هم اجرا شود چیزی را خراب نمی‌کند و
ویرایش‌های شما را بازنویسی نمی‌کند).
