# تحویل پروژه — «از همین‌جا ادامه بده»

این سند برای لحظه‌ای نوشته شده که هیچ‌کس از سابقه‌ی پروژه چیزی نداند: نه توسعه‌دهنده‌ی
تازه، نه مدل هوش مصنوعی دیگری، نه خودِ مالک شش ماه بعد. هر چیزی که برای ادامه دادن
لازم است یا این‌جاست یا از این‌جا نشانی داده شده.

**قاعده‌ی نگهداری این فایل:** هر تغییری که یکی از این‌ها را عوض کند، در همان کامیت
این فایل را هم عوض می‌کند — بخش «چه چیزی ساخته شده»، «چه چیزی نه»، یا هر کدام از
قاعده‌های معماری. سندی که با کد جلو نرود، بعد از دو ماه بدتر از نبودنش است، چون
دروغ می‌گوید و کسی که به آن اعتماد کند وقتش را از دست می‌دهد.

آخرین به‌روزرسانی: شهریور ۱۴۰۵ — بعد از بازنویسی UX بازار خودرو و یکسان شدن
فیلترهای هر سه بازار (فیلتر تاشو، برند و مدل چندانتخابی، ترتیب نتیجه‌ها).

---

## ۱. محصول در یک بند

نمایندگی‌های خودرو در ایران بین خودشان حواله و ظرفیت ثبت‌نام معامله می‌کنند. این
سامانه جای آن معامله است: هر نمایندگی آگهی می‌گذارد، آگهی‌های بقیه را می‌بیند، و
**شماره‌ی تماس صاحب آگهی پنهان است** تا وقتی که آن را «باز کند» — که از سهمیه‌ی
روزانه‌اش کم می‌شود و برای همیشه ثبت می‌شود. درآمد از اشتراک ماهانه‌ی نمایندگی‌هاست.

دو پنل: **نماینده** و **مدیریت**. یک نقش سوم هم هست — **مالک** — که از دید همه‌ی
نقش‌های دیگر اصلاً وجود ندارد.

---

## ۲. وضعیت امروز

### ساخته شده و روی سرور کار می‌کند

- **بازار حواله** — ثبت آگهی فروش و درخواست خرید، استعلام با فیلتر، تمدید، «فروخته شد».
- **بازار ثبت‌نامی** — همان چرخه برای ظرفیت ثبت‌نام کارخانه. ماژول مستقل.
- **پنهان‌سازی مشخصات** — شماره تا لحظه‌ی «باز کردن» هرگز از سرور بیرون نمی‌رود.
  سهمیه‌ی روزانه/ماهانه، مشترک بین هر سه بازار.
- **اشتراک و حالت ماژول** — پلن‌ها، صورتحساب، زیرنمایندگی، خرید ظرفیت با فیش واریزی.
- **گزارش تخلف** — با اخطار، و تعلیق خودکار بعد از سومین تخلف تأییدشده.
- **جلوگیری از دور زدن سیستم بازدید** — قاعده‌ی اصلی این است: **هیچ جعبه‌ی
  تایپی روی کارت عمومی سرو نمی‌شود.** توضیحات، نام طرح، شرایط و موعد تحویل بعد
  از «نمایش مشخصات» می‌آیند. کارت فقط فیلدهای ساختاریافته را دارد، که در آن‌ها
  نمی‌شود چیزی پنهان کرد. پشتش هم رد کردن الگوهای قطعی موقع ثبت، پوشاندن موقع
  خواندن، فهرست رفتاری «آگهی زیاد، بازدید صفر»، و یک دلیل تخلف برای گزارش.
  `tests/e2e/textLeak.test.js` هر ستون متنی را خودش پیدا می‌کند — فیلد جدید
  بدون این‌که کسی چیزی اضافه کند پوشش داده می‌شود.
- **پشتیبانی** — تیکت با پیوست، دسته‌بندی، اولویت.
- **صندوق اطلاعیه‌ی نمایندگی** — شامل تعلیقِ مستقیمِ آگهی توسط مدیر. هر تصمیمی که سامانه درباره‌ی حساب یا آگهی‌های
  یک نمایندگی گرفته، با تاریخ و دلیل و دکمه‌ی اعتراض. جدولی ندارد: از روی همان
  گزارش‌های تخلف ساخته می‌شود و فقط «آخرین بار کِی باز شده» ذخیره می‌گردد.
- **ویرایش آگهی** — همه‌چیز جز خودرو و نوع آگهی. آشکار است: نشان «ویرایش‌شده» روی
  کارت، تغییرِ فیلدبه‌فیلد در لاگ کارها، و اطلاعیه برای هر کسی که قبلاً برای دیدن
  مشخصات همان آگهی سهمیه خرج کرده بود.
- **صفحه‌بندی شماره‌دار** — هر چهار فهرست (استعلام حواله، استعلام ثبت‌نامی، و
  «آگهی‌های من» در هر دو بازار) شماره‌ی صفحه دارند و تعداد کل را برمی‌گردانند.
  پیش از این «آگهی‌های من» فقط یک `limit` می‌گرفت: نمایندگی‌ای با شصت آگهی،
  پنجاه‌تا می‌دید و هیچ‌جا نمی‌گفت ده‌تای دیگری هم هست — و فهرستی که بی‌صدا کوتاه
  باشد یعنی «همینه». سقف عمق `MAX_PAGE = 50` است (صفحه‌ی افستی، هرچه عمیق‌تر
  گران‌تر)؛ پیجر از آن جلوتر دکمه نمی‌سازد و روی آخرین صفحه می‌نویسد که راهِ
  رسیدن به بقیه، باریک‌تر کردن فیلتر است نه صفحه‌ی عمیق‌تر. مسیر cursor برای
  خروجی و کارهای پس‌زمینه سر جایش است و در هر عمقی یک‌سرعت می‌ماند.
- **بازار خودرو** — خودروی آماده، صفر یا کارکرده، بین نمایندگی‌ها. همان هسته
  (پنهان‌سازی تماس، سهمیه‌ی مشترک، گزارش، اطلاعیه، میز مدیریت از رجیستری) با
  چهار تفاوت خودش: **«وضعیت بدنه»** — جدول ۲۲ قطعه با گزینه‌های مخصوص هر
  کلاس قطعه که روی آگهی به شکل نقشه‌ی سه‌نمایی با نقطه‌های رنگی دیده می‌شود
  (`ui/bodyMap.js`؛ برگه‌های اصلی مالک در `assets/body/sheets/`، مرز برش‌ها از
  روی پیکسل‌ها اندازه‌گیری شده و `scripts/crop-body-sheets.py` از همان مرزها
  فایل‌های کوچک webp هر نما را می‌سازد — صفحه فقط همین‌ها را بار می‌کند، نه
  برگه‌ی ۹۰۰ کیلوبایتی؛ اگر برگه یا مرزی عوض شد اسکریپت را دوباره بزنید)؛ خلاصه‌ی
  یک‌کلمه‌ای (بدون رنگ/رنگ جزئی/رنگ‌شده/تعویض‌دار/شاسی‌خورده) از روی جدول
  **مشتق** می‌شود و فیلتر جستجوست — فیلترهای «وضعیت بدنه» و «نوع بدنه»
  چندانتخابی‌اند (مثلاً رنگ‌شده + تعویض‌دار) و در آدرس با کاما می‌آیند؛
  جدولِ ۲۲ قطعه در فرم ثبت **پنج گروه تاشده** است (گلگیرها، درب‌ها، کاپوت و
  صندوق و سقف، شاسی و سرشاسی، پالونی و سینی) و هر گروه تعداد علامت‌خورده‌ی
  داخلش را روی سربرگ می‌گوید؛ نقشه را هم **زنده** زیر دست فروشنده می‌کشد؛
  ترتیب نتیجه‌ها (`sort=new|cheap|expensive|km`) پارامتر واقعی سرور است و
  NULLها آخر می‌آیند تا «ارزان‌ترین» با درخواست‌های بی‌قیمت شروع نشود؛
  **گارانتی (فعال/غیرفعال)** روی آگهی فروش الزامی است و فیلتر خودش را دارد
  («فقط گارانتی فعال»)؛ درخواست خرید نه بدنه دارد نه گارانتی؛ **نوع بدنه از کاتالوگ می‌آید نه از
  فروشنده** (ستون `bodyType` روی CarModel، کلاس‌بند خودکار در بوت فقط
  ردیف‌های null را پر می‌کند، اصلاح دستی از ویرایشگر کاتالوگ؛ قواعدش مدل‌به‌مدل
  روی کل کاتالوگ بازبینی شده و درس‌های regex فارسی بالای
  `catalog/bodyClassifier.js` نوشته شده — اگر قاعده‌ای را عوض کردید، یک
  مایگریشن که ستون را null کند لازم است تا بوت بعدی همه را از نو بزند)؛ **عکس‌ها
  (تا ۶) پشت «نمایش مشخصات»** — روی شیشه می‌شود شماره نوشت، پس عکس مثل متن
  آزاد است: نام فایل تصادفی، سروِ آن پشت مجوز (مالک/مدیر/بازکننده‌ی
  ثبت‌شده)؛ و **بدون محدودیت برند** در هر دو سمت — به خواست مالک، خودروی
  توی پارکینگ مال هر کسی است که بفروشد.
- **کاتالوگ خودرو** — ۱۸۶ برند، ۲۰۰۰+ مدل، قابل ویرایش از پنل؛ حالا با
  **نوع بدنه** روی هر مدل (سدان/هاچبک/شاسی‌بلند/وانت).
- **پنل مدیریت** — نمایندگی‌ها، یک میز جدا برای هر بازار (حواله، ثبت‌نامی،
  خودرو — یک کامپوننت با جدول `MARKETS` در `pages/admin/index.js`)، گزارش‌ها،
  تنظیمات، کاربران سیستم.
- **فیلترهای هر سه بازار پشت یک «فیلترها»** — `ui/filterBox.js`: یک
  `<details>` بومی، بدون کلید کلیک و بدون state در استور؛ روی گوشی بسته است
  (پنل قبلاً کل صفحه‌ی اول بود و نماینده قبل از دیدن یک آگهی باید از تمام
  فرم رد می‌شد)، اگر فیلتری فعال باشد خودش باز می‌ماند، و روی صفحه‌ی بزرگ باز
  است با دستگیره‌ی مخفی. باز بودنش را خود صفحه از عرض پنجره تصمیم می‌گیرد
  (`wideScreen()`) چون `<details>` بسته محتوایش را با `content-visibility`
  پنهان می‌کند و CSS روی فرزند به آن نمی‌رسد.
- **«برند و مدل» چندانتخابی است** — همان `ui/brandPicker.js` پنل مدیریت، این
  بار برای فیلتر: تیک برند یعنی کل برند، باز کردن برند و تیک چند مدل یعنی
  همان مدل‌ها. در آدرس به شکل `brandIds=` و `carModelIds=` با کاما می‌رود
  (`utils/queryList.js` کلمه‌به‌کلمه اعتبارسنجی می‌کند — ورودی ناشناخته ۴۲۲
  می‌گیرد، بی‌صدا حذف نمی‌شود) و برای برگرداندن تیک‌ها از آدرسِ اشتراکی
  `GET /catalog/models?ids=…` برند هر مدل را می‌گوید. خود فهرست برندها داخل
  یک `<details>` دیگر (`ui/brandFilter.js`) تا شده — ۱۸۶ برند باز، پنل فیلتر
  را به کاتالوگ تبدیل می‌کرد.
- **چیپ‌های فیلتر خودشان چک‌باکس‌اند** — `ui/checkChips.js`؛ نسخه‌ی قبلی ردیفی
  از دکمه بود و مثل انتخاب تک‌گزینه‌ای خوانده می‌شد.
- **آدرس API در مرورگر هم صفحه‌ی خطا را می‌بیند، نه JSON** — `errorHandler`
  از روی `Accept` تصمیم می‌گیرد: مرورگرِ در حال ناوبری `text/html` می‌خواهد و
  به `/error.html` هدایت می‌شود؛ `fetch` پنل «هرچیزی» می‌خواهد و همان JSON
  با کد ماشین‌خوان را می‌گیرد. این یک مرز امنیتی نیست — هیچ‌چیز این‌جا تصمیم
  نمی‌گیرد چه چیزی مجاز است — فقط شکلِ «نه» را انتخاب می‌کند. شمارش تهدید
  قبل از آن انجام می‌شود، پس اسکنر در هر دو شکل ثبت می‌شود.
- **آدرسی که وجود ندارد، صفحه‌ی خودمان را می‌بیند** — `frontend/error.html`
  یک صفحه‌ی مستقل است (بدون CSS و JS برنامه، چون همان‌ها ممکن است دلیل خطا
  باشند) با دکمه‌ی «بازگشت به صفحه‌ی اصلی»، و nginx آن را برای ۴۰۳ و ۴۰۴ خودش
  سرو می‌کند. `autoindex off` صریح نوشته شده تا هیچ پوشه‌ای فهرست نشود.
  مسیرهای `/api/` عمداً به این صفحه نمی‌روند: تماس‌گیرنده‌ی آن‌جا یک برنامه
  است و HTML جواب قابل‌پردازشی نیست.
- **سربرگ‌های امنیتی در `deploy/nginx/security-headers.inc`** — nginx
  `add_header` را به بلاکی که سربرگ خودش را دارد به ارث نمی‌دهد، و
  `location /assets/` و `/src/` هر دو `Cache-Control` دارند؛ یعنی تا پیش از
  این، فایل‌های جاوااسکریپت و CSS بدون CSP و بدون `nosniff` سرو می‌شدند.
  هر `location` که سربرگی اضافه می‌کند باید این فایل را `include` کند.
- **متن‌های هر بخش مال همان بخش‌اند** — پیام‌های مشترک بازارها در
  `MESSAGES.LISTING` («آگهی…») است؛ `MESSAGES.HAVALE` فقط برای ماژول حواله.
  ماژول جدید نباید از پیام‌های حواله قرض بگیرد.
- **داشبورد مدیریت** — «کارتابل» اول صفحه: هر صفِ معطل با عدد، **سنِ
  قدیمی‌ترین کارِ معطل**، و دکمه‌ی مستقیم. مرتب بر اساس فوریت (نه ترتیب ثابت) و
  رنگ از روی سن می‌آید نه از نوع کار.
  بعد وضعیت بازار (به تفکیک بازار)، و دو رتبه‌بندی که جای دیگری نیستند:
  پرتقاضاترین خودروها و فعال‌ترین نمایندگی‌ها. تحلیل‌های رفتاری صفحه‌ی خودشان
  را دارند («رفتار قابل بررسی») چون بررسی‌اند نه چکِ صبحگاهی.
- **لاگ کارها** — چه کسی چه کرد، با «از چه به چه» روی ویرایش‌ها، آی‌پی و دستگاه، و جستجو.
- **لاگ فنی** (پنل مالک) — خطاها و درخواست‌های کند.
- **لاگ امنیتی** (پنل مالک) — تلاش‌های نفوذ، اسکن، حدس رمز؛ با امکان بستن آی‌پی.
- **نگهداری خودکار** — کار شبانه: آرشیو NDJSON، بعد پاک کردن، با تأیید تعداد خط.
- **زیرساخت** — Docker Compose، nginx با TLS، بکاپ کامل با چرخش، هشدار تلگرام،
  اسکریپت تشخیص کندی.

### ساخته نشده

- **بازار قطعات** — در منو با برچسب «به‌زودی». معماری برایش آماده است (بخش ۳)؛
  بازار خودرو با همین الگو ساخته شد و نمونه‌ی خوبی برای شروع آن است.
- **نقش DEVELOPER** — در enum هست، دسترسی‌هایش عمداً خالی است.
- **اعلان PWA**.
- حدود **۷۰ برند بدون لوگو**.

### تصمیم‌های به تعویق افتاده‌ی مالک

اینها بررسی شده‌اند و مالک آگاهانه گفته «فعلاً نه». دوباره پیشنهادشان ندهید مگر
شرایط عوض شود:

- `DATA_ENCRYPTION_KEY` — رمزنگاری ستون‌های حساس در حالت فعلی.
- بکاپ خارج از سرور (off-site).

### کارهای باز

- `SEED_DEMO` روی سرور تولید هنوز `true` است. چهار حساب نمونه با رمز عمومی
  `Demo@12345` در هر دیپلوی دوباره ساخته می‌شوند. **پیش از اولین نمایندگی واقعی
  باید `false` شود و آن حساب‌ها تعلیق شوند** — [`launch-checklist.md`](launch-checklist.md).
- فرانت‌اند ۳۰ ماژول جدای بدون build است. روی جابه‌جایی بین صفحه‌ها اثر ندارد، ولی
  هر بار بارگذاری کامل صفحه یعنی ۳۰ درخواست شرطی. یک مرحله‌ی esbuild حلش می‌کند.

---

## ۳. قاعده‌های معماری — شکستنشان بی‌صدا خراب می‌کند

این‌ها سلیقه نیستند. هر کدام از یک اشتباه واقعی در همین پروژه بیرون آمده، و
شکستنشان چیزی را خراب می‌کند که تست نمی‌گیردش.

### ۳.۱ بازارها ماژول‌اند و هسته هیچ‌کدام را نمی‌شناسد

یک جدول `Listing` با ستون `market`، و برای هر بازار یک جدول جزئیات
(`RegistrationDetail`). رفتار مشترک — باز کردن مشخصات، گزارش، تعلیق — در
`modules/listing/` است.

هسته **هیچ‌وقت** یک بازار را import نمی‌کند. هر بازار خودش را در
`modules/listing/marketRegistry.js` معرفی می‌کند:

```js
registerMarket('REGISTRATION', { label, include, summarise, describe });
```

میز مدیریت از رجیستری می‌پرسد و هرچه بازارها گفته‌اند نمایش می‌دهد. سرور
`fields: [{label, value, money?, date?, icon?}]` برمی‌گرداند و **پنل حق ندارد
بداند این فیلدها یعنی چه** — انتخاب آیکون از روی برچسب فارسی، دقیقاً همان
وابستگی‌ای است که این رجیستری حذفش کرده.

**افزودن بازار خودرو = یک پوشه‌ی جدید + یک خط در منو.** هیچ فایل مشترکی عوض نمی‌شود.

### ۳.۲ مشخصات تماس تا لحظه‌ی ثبت، سریالایز نمی‌شود

نه «در پنل قایمش کن». اصلاً از سرور بیرون نمی‌رود. DTOهای `*.dto.js` این را
تضمین می‌کنند و اسموک‌تست کل سند را می‌گردد و اگر شماره‌ای بیرون از کارتی که واقعاً
باز شده پیدا کند، قرمز می‌شود. **اگر آن مرحله قرمز شد، با آن مثل یک حادثه رفتار
کنید.**

### ۳.۳ `ui/html.js` پیش‌فرض escape می‌کند

هر مقداری که در markup تزریق شود escape می‌شود، مگر با `raw()`. نسخه‌ی اولیه در هر
محل `esc()` صدا می‌زد — که تا وقتی کسی یادش نرود کار می‌کند. حالا اشتباه، یک
`raw()` قابل دیدن در diff است، نه یک غیبتِ نامرئی.

### ۳.۴ اجزای داخل فرم، حالتشان در DOM است نه در store

هر نوشتن در store، کل صفحه را از نو می‌سازد و هر چیزی که کاربر تایپ کرده پاک
می‌شود. انتخابگر برند، انتخابگر تاریخ شمسی، و pickSelect همه در DOM کار می‌کنند.

### ۳.۵ `CLICK_KEYS` در `main.js`

یک شنونده‌ی کلیک برای کل برنامه. هر `data-key` تازه **باید** در آن مجموعه ثبت شود.
کلیکی که ثبت نشده باشد، در سکوت کامل هیچ کاری نمی‌کند — یک صفحه دکمه که هیچ‌کدام
کار نمی‌کنند و هیچ خطایی هم نیست.

### ۳.۶ خطای اعتبارسنجی ۴۲۲ است، نه ۴۰۰

### ۳.۷ تاریخ‌ها شمسی‌اند، در نمایش و در ورودی

`ui/format.js` با `fa-IR-u-ca-persian`، و `ui/dateInput.js` برای ورودی. تبدیل از
خودِ `Intl` پرسیده می‌شود نه از جدول کبیسه‌ی دستی — تا تقویمِ ورودی و تقویمِ نمایش
هیچ‌وقت با هم اختلاف پیدا نکنند.

### ۳.۸ مالک در هیچ لاگی نیست

نقش `OWNER` هیچ ردیف لاگی نمی‌نویسد — نه فیلتر می‌شود موقع خواندن، اصلاً نوشته
نمی‌شود. استثنا فقط `LOGIN_FAILED` است، چون قفل خودکار حساب به آن نگاه می‌کند.
**نام کاربری حساب مالک هیچ‌جا در کد، سند یا گفتگو نوشته نمی‌شود.**

### ۳.۹ هر کوئری لاگ، بازه‌ی زمانی دارد

`count` بی‌قید روی جدول لاگ یعنی اسکن کامل — که تا وقتی جدول کوچک است دیده نمی‌شود
و هر ماه بدتر می‌شود.

### ۳.۱۰ بستن آی‌پی هرگز خودکار نیست

آدرس از هدری می‌آید که خودِ کلاینت تعیینش می‌کند. سیستمی که خودکار می‌بندد را
می‌شود وادار کرد نمایندگی‌های واقعی را بیرون بیندازد — ابزار دفاع، سلاح حمله
می‌شود.

### ۳.۱۱ تنبیه باید برای تنبیه‌شده خواندنی باشد

نمایندگی تعلیق‌شده وارد می‌شود. `resolveAccess` برای هر حساب غیرفعال هیچ
اختیاری نمی‌دهد، پس همه‌ی درها بسته می‌مانند — ولی صفحه‌ها باز است تا دلیل و
شمار اخطارها را بخواند و اعتراض ثبت کند. کارمند تعلیق‌شده وارد نمی‌شود؛ آن یک
واژه است برای دو رویداد متفاوت. به همین دلیل `/notices` هم نگهبان اشتراک ندارد:
توضیحِ تعلیق نباید پشت خودِ تعلیق باشد.

---

## ۴. قاعده‌های امنیتی که مالک گذاشته

- **هیچ رمز، کلید API یا رازی در گفتگو فرستاده نمی‌شود.**
- **نام کاربری حساب مالک هیچ‌جا نوشته نمی‌شود** — نه در کد، نه در سند، نه در چت.
- رمزهای تولید هرگز کامیت نمی‌شوند. ابزار تست بار، حساب‌ها را از فایلی بیرون از
  مخزن می‌خواند (`--accounts-file`).

---

## ۵. روش کار

- توسعه روی شاخه‌ی `claude/delegation-platform-phase-one-0xfqz7`.
- داده‌ی نمونه روی ماشین توسعه: `npm run seed` (پلن و کاتالوگ)، بعد
  `npm run seed:demo` (چهار نمایندگی نمونه و چند حواله)، بعد `npm run seed:cars`
  (ده آگهی خودرو با هر چهار نوع بدنه، از صفرکیلومتر تا شاسی‌خورده، سه‌تا با
  عکس جای‌گیر). هر سه از مسیر خود سرویس‌ها می‌روند، تکراری نمی‌سازند و در
  تولید بدون `ALLOW_DEMO_SEED=true` اجرا نمی‌شوند — که نباید باشد. روی سرور
  `npm` نیست؛ داخل کانتینر: `docker compose exec api node scripts/seed-cars.js`
  (با `SEED_DEMO=true` در بوت خودش اجرا می‌شود).
- **مالک خودش دیپلوی می‌کند** (`./deploy/update.sh` روی سرور) و خروجی را برمی‌گرداند.
  توسعه‌دهنده دسترسی تولید ندارد.
- زبان: رابط کاربری و اسناد فارسی؛ توضیحات داخل کد انگلیسی.
- توضیحات کد **چرایی** را می‌نویسند نه چیستی — این پروژه عمداً این‌طور نوشته شده و
  بیشترین ارزشش همان‌جاست. ادامه‌اش بدهید.

---

## ۶. کجا دنبال چه بگردید

| سؤال | جواب |
|---|---|
| بازار جدید اضافه کنم؟ | `modules/havale/havale.market.js` را الگو بگیرید |
| چرا این‌طوری نوشته شده؟ | توضیح بالای همان فایل. تقریباً همیشه هست |
| نیازمندی اصلی محصول؟ | [`blueprint.md`](blueprint.md) |
| سرور را از صفر بالا بیاورم؟ | [`deployment.md`](deployment.md) |
| لاگ‌ها چطور طراحی شده‌اند؟ | [`monitoring-design.md`](monitoring-design.md) |
| قبل از لانچ چه کنم؟ | [`launch-checklist.md`](launch-checklist.md) |
| سایت کند شده؟ | `./deploy/panel-check.sh --account admin` |
| چه چیزی کند بوده؟ | پنل مالک ← لاگ فنی ← درخواست‌های کند |
| ورودی پول کجا ساخته می‌شود؟ | `frontend/src/ui/moneyInput.js` — هر قیمتی باید از آن رد شود |
| نمایندگی چطور می‌فهمد چرا اخطار خورده؟ | `modules/notice/` روی سرور، `pages/agent/notices.js` در پنل |
| جلوی شماره در متن آگهی چطور گرفته می‌شود؟ | با سرو نکردنش: متن تایپی پشت «نمایش مشخصات» است (`*.dto.js`). `utils/textGuard.js` لایه‌ی دوم است |

---

## ۷. سلامت پروژه در این لحظه

- **۵۰۰ تست بک‌اند** (`RUN_E2E=1 npm test`) — سبز
- **۵۴ مرحله اسموک مرورگری** — سبز
- **lint** — تمیز
- زمان پاسخ سرور تولید: ۳ تا ۲۳ میلی‌ثانیه در هر صفحه‌ی پنل
- دیتابیس: ۱۰۰٪ اصابت حافظه، بدون کوئری کند

اگر این عددها بعد از تغییر شما بدتر شدند، همان تغییر مقصر است — قبل از ادامه
درستش کنید.
