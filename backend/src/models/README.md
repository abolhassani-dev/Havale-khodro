# models/

**Responsibility:** Schema definitions

ORM models, relations, indexes, schema-level validation.

**Does not belong here:** Behaviour. Structure only — logic lives in services.

---
_Part of the havale architecture standard. See `backend/README.md` for the full layout._
