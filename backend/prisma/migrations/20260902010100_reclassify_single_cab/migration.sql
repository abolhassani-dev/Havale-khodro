-- و همه‌ی وانت‌های تک‌کابینِ کاتالوگ دوباره دسته‌بندی می‌شوند.
--
-- ستون را null می‌کند تا کلاس‌بندِ بوت بعدی از نو بزندش — همان قراری که بالای
-- catalog/bodyClassifier.js نوشته شده: قاعده که عوض شد، ردیف‌های null دوباره
-- خوانده می‌شوند. فقط وانت‌ها، چون قاعده‌ی عوض‌شده فقط درباره‌ی آن‌هاست و
-- دسته‌ی دستیِ کسی روی سدان و شاسی‌بلند نباید پاک شود.
--
-- در دو مایگریشن جدا از افزودن مقدار enum: پستگرس اجازه نمی‌دهد مقدار تازه‌ی
-- یک enum در همان تراکنشی که ساخته شده استفاده شود.
UPDATE "CarModel" SET "bodyType" = NULL WHERE "bodyType" = 'PICKUP';
