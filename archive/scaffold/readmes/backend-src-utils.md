# utils/

**Responsibility:** Pure helpers

Small dependency-free functions: dates, strings, numbers, hashing wrappers, formatters, the logger.

**Does not belong here:** Anything needing config, a model, or the database. The test: a util should move to another project unchanged.

---
_Part of the havale architecture standard. See `backend/README.md` for the full layout._
