{
  "name": "feranocar-frontend",
  "private": true,
  "description": "The panel itself has no dependencies and no build step — see README.md. This file exists only so the browser test suite can be installed and run reproducibly.",
  "scripts": {
    "dev": "node tests/dev-server.js",
    "smoke": "node tests/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
